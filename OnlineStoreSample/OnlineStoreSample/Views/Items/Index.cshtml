@model IEnumerable<OnlineStoreSample.Models.Item>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Items</h2>

@foreach (var item in Model)
{
    <li>@Html.ActionLink(item.Name,	"Detail", "Items", new { id = item.Id }, null)</li>
}

